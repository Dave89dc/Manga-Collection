import { CollectionIfIsCompletePipe } from './collection-if-is-complete.pipe';

describe('CollectionIfIsCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new CollectionIfIsCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
