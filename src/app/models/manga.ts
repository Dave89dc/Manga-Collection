export interface Manga {
  title: string;
  imageUrl: string;
  author: string;
  originalRun: string;
  publishedBy: string;
  genre: string;
  plot: string;
  volumes: number;
  isComplete: boolean;
}
